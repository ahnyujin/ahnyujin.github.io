---
title: 함수형 프로그래밍
author: Yujin Ahn
date: 2020-11-05 17:24:00 +0900
categories: [CS, etc]
tags: [etc]     # TAG names should always be lowercase
---
# Functional Programming
순수함수를 조합하여 프로그래밍하는 방식

함수형 프로그래밍은 계산을 수학적 함수의 조합으로 생각하는 방식을 말한다. 이것은 일반적인 프로그래밍 언어에서 함수가 특정 동작을 수행하는 역할을 담당하는 것과는 반대되는 개념으로, 함수를 수행해도 함수 외부의 값이 변경될 수 없다.

함수형 프로그래밍은 두가지 특징을 따른다.
1. 불변성 (immutable)
    - 데이터가 변경될 수 없다. 데이터를 변경하고 싶은 경우 원본 데이터 구조를 변경하지 않고 그 데이터를 복사본을 만들어 그 일부를 변경하고, 변경한 복사본을 사용해 작업을 진행한다.
2. 1급객체 (first-class object)
   - functional programming에서 함수는 일급객체로 간주된다.
   - 일급객체란? 변수나 객체에서 함수의 파라미터로 전달할 수 있고, 함수를 리턴할 수 있다.

# 용어정리
순수함수란?
- 동일한 입력에는 항상 같은 값을 반환한다.
- 함수의 실행이 외부에 영향을 끼치지 않아야 한다. //thread safe하고, 병렬적 계산이 가능하다.

# 출처
-[함수형 프로그래밍 요약](https://velog.io/@kyusung/%ED%95%A8%EC%88%98%ED%98%95-%ED%94%84%EB%A1%9C%EA%B7%B8%EB%9E%98%EB%B0%8D-%EC%9A%94%EC%95%BD)